apiVersion: v1
kind: ConfigMap
metadata:
    name: core-config
    labels:
        app: fiubook-core
data:
    DATABASE_HOST: postgres-db-internal
    DATABASE_PORT: '5432'
    DATABASE_USER: sysuser
    DATABASE_PASSWORD: pistacho
    DATABASE_NAME: fiubook-core-db
    PAGINATION_SECRET: yerbamate
    SESSION_SECRET: playadito
    SESSION_DEFAULT_DURATION: '7 days'
    LISTENING_PORT: '3000'
    PAGE_SIZE: '10'
